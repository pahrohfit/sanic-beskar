version: 2

python:
  install:
    - requirements: docs/requirements.txt
    #- requirements: requirements.txt
    - method: pip
      path: .
  system_packages: true

build:
  os: ubuntu-22.04
  tools:
    python: "3.7"
  commands:
    - pip install poetry
    - poetry install
