version: 2

python:
  install:
    - requirements: docs/requirements.txt
    #- requirements: requirements.txt
    - method: pip
      path: .
  system_packages: true
build:
  commands:
    - pip install poetry
    - poetry install
